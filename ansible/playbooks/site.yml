- name: Configure Docker hosts
  hosts: docker_hosts
  become: true

  roles:
    - docker
    - demo_app

- name: Configure reverse proxy
  hosts: dmz
  become: true

  roles:
    - docker
    - traefik
